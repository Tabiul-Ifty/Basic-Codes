#include<stdio.h>
#include<string.h>
int main ()
{
    char str[100]="Bangladesh";
    int len=strlen(str);

    printf("string is = %s\n",str);
    printf("strlen = %d ",len);

    return 0;
}
